import { Matcher } from "../types";
/**
 * Matches at random
 * @param chance Chance of matching, between 0 and 1
 * @example
 * matchRandomly()
 *
 * // Matching: "Hi"
 * // Not matching: "Hi"
 * // Not matching: "Hi"
 * // Matching: "Hi"
 * @category Matching
 */
export declare const matchRandomly: (chance?: number) => Matcher<{}, any, any>;
