"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Matches the first satisfied matcher
 * @example
 * matchAny(matchRegex(/[Hh]ey!/), matchPrefixes("hi"))
 *
 * // Matching: "hey!"
 * // Matching: "hi"
 * // Matching: "Hey!"
 * // Not matching: "hello"
 * @category Matching
 */
exports.matchAny = (...matchers) => (context) => __awaiter(this, void 0, void 0, function* () {
    for (const matcher of matchers) {
        const result = yield matcher(context);
        if (result)
            return result;
    }
    return;
});
//# sourceMappingURL=matchAny.js.map