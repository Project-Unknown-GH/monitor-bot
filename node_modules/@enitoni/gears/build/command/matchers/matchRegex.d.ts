import { Matcher } from "../types";
/**
 * Can be RegExp or () => RegExp
 * @category Internal
 */
export declare type RegExpResolvable = RegExp | (() => RegExp);
/**
 * @ignore
 */
export interface RegexData {
    match: RegExpExecArray;
}
/**
 * Match when the provided regular expression matches, adds match array to [[Context]] state.
 * @example
 * matchRegex(/^[Hh]elp( me)?!?$/)
 *
 * // Matching: "Help!"
 * // Matching: "help me"
 * // Not matching: "Please help me"
 * @category Matching
 */
export declare const matchRegex: (regex: RegExpResolvable) => Matcher<RegexData, any, any>;
