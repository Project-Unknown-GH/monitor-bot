import { Context, Middleware } from "../types";
/**
 * @ignore
 */
export declare const composeMiddleware: (chain: Middleware<{}, any, any, any>[]) => (context: Context<any, any, any>, next?: Middleware<{}, any, any, any> | undefined) => Promise<any>;
