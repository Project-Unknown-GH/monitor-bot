/**
 * This composes a chain and manages context to make sure
 * each context is correct based on the level in the chain
 * @ignore
 */
export declare const composeChain: (chain: import("../types").ChainEntry<any, any>[]) => () => Promise<any>;
