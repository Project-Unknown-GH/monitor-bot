"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @ignore
 */
exports.emitOrThrow = (emitter, type, error) => {
    if (emitter.hasListeners(type)) {
        emitter.emit(type, error);
    }
    else {
        console.error(`Uncaught "${type}" event. Use ${emitter.constructor.name}.on("${type}", ...) to catch these error events safely.`);
        throw error;
    }
};
//# sourceMappingURL=emitOrThrow.js.map